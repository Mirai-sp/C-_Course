namespace Primeiro.Capitulo6.Exemplos.Structs
{
    struct Point
    {
        public double X, Y;
        public override string ToString()
        {
            return "(" + X + "," + Y + ")";
        }
    }
}
